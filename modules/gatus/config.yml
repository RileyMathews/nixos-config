web:
  port: 8020

storage:
  type: postgres
  path: "postgres://gatus:${POSTGRES_PASSWORD}@pg17.tailscale.rileymathews.com:5432/gatus?sslmode=disable"


endpoint-defaults: &defaults
  group: Services
  interval: 30s
  client:
    timeout: 10s
  conditions:
    - "[STATUS] == 200"
    - "[CERTIFICATE_EXPIRATION] > 72h"
  alerts:
    - type: ntfy

alerting:
  ntfy:
    topic: "home-server-alerts"
    priority: 3
    url: "https://ntfy.rileymathews.com"
    default-alert:
      failure-threshold: 3
      send-on-resolved: true
      minimum-reminder-interval: 1h


endpoints:
  - name: rpgweave
    <<: *defaults
    url: "https://rpgweave.com/health_check"

  - name: vaultwarden
    <<: *defaults
    url: "https://vaultwarden.rileymathews.com"

  - name: homeassistant
    <<: *defaults
    url: "https://home.rileymathews.com"

  - name: mealie
    <<: *defaults
    url: "https://mealie.rileymathews.com"

  - name: forgejo
    <<: *defaults
    url: "https://git.rileymathews.com"

  - name: karakeep
    <<: *defaults
    url: "https://karakeep.rileymathews.com"

  - name: miniflux
    <<: *defaults
    url: "https://miniflux.rileymathews.com"

  - name: searxng
    <<: *defaults
    url: "https://search.rileymathews.com"

  - name: audiobookshelf
    <<: *defaults
    url: "https://audiobookshelf.rileymathews.com"

  - name: registry
    <<: *defaults
    url: "https://registry.rileymathews.com/v2/"

  - name: immich
    <<: *defaults
    url: "https://immich.rileymathews.com"

  - name: paperless
    <<: *defaults
    url: "https://paperless.rileymathews.com"

  - name: homebridge
    <<: *defaults
    url: "https://homebridge.rileymathews.com"

  - name: ntfy
    <<: *defaults
    url: "https://ntfy.rileymathews.com"

  - name: joplin
    <<: *defaults
    url: "https://joplin.rileymathews.com"

  - name: vikunja
    <<: *defaults
    url: "https://vikunja.rileymathews.com"

  - name: buffer
    <<: *defaults
    url: "https://buffer.rileymathews.com"

  - name: davhome
    <<: *defaults
    url: "https://davhome.rileymathews.com"

  - name: homebox
    <<: *defaults
    url: "https://homebox.rileymathews.com"

  - name: grafana
    <<: *defaults
    url: "https://grafana.rileymathews.com"

  # - name: jellyfin
  #   <<: *defaults
  #   url: "https://jellyfin.rileymathews.com"
  #
  - name: copyparty
    <<: *defaults
    url: "https://copyparty.rileymathews.com"

external-endpoints:
  - name: thegenerosityco-staging-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: forgejo-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: homebridge-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: vikunja-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: vaultwarden-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: homebox-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: paperless-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: homeassistant-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: karakeep-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: mealie-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h

  - name: pinchflat-backup
    group: Backups
    token: "$GATUS_PUSH_TOKEN"
    heartbeat:
      interval: 25h
